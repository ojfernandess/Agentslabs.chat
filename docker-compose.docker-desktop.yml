# Override opcional: expõe a app em http://localhost:3000
# Preferível para uso local: docker-compose.local.yml (um comando só).
# Alternativa: docker compose -f docker-compose.easypanel.yml -f docker-compose.docker-desktop.yml up -d
services:
  app:
    ports:
      - '3000:3000'
